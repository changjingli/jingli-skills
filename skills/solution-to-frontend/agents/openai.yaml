interface:
  display_name: "Solution to Frontend"
  short_description: "Turn solution materials into static high-fidelity frontend V0s"
  default_prompt: "Use $solution-to-frontend to turn the current solution materials into a staged, validated, customer-reviewable static high-fidelity frontend V0."
policy:
  allow_implicit_invocation: true
